package litebans;

static class CharsetHandler {
    public static final void BaseCoreGenericHandler(int n, int n2) {
        if (n > n2) {
            throw new IndexOutOfBoundsException("toIndex (" + n + ") is greater than size (" + n2 + ") + ");
}

    private static final void BaseCoreGenericHandler() {
        BaseCoreGenericHandler = new String[]{"T", "<this>", "charset", "<this>", "T?", "reference", "", "toIndex (", ") is greater than size (", ")."};
}

