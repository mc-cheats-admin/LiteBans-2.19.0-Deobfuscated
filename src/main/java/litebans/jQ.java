package litebans;

import litebans.hl;
import litebans.j8;

static class jQ
extends j8 {
    private static final int c = 0x40000000;
    private static /* synthetic */ String[] b;

    public static final int a(int n) {
        return n < 0 ? n : (n < 3 ? n + 1 : (n < 0x40000000 ? (int)((float)n / 0.75f + 1.0f) : Integer.MAX_VALUE));
    }

    private static final void b() {
        b = new String[]{"pair", "builderAction", "builderAction", "builder", "<this>", "defaultValue", "<this>", "<this>", "comparator", "pairs", "comparator", "pairs", "<this>", "<this>", "<this>"};
    }

    static {
        jQ.b();
    }
}

