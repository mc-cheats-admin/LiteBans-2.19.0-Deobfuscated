package litebans;

import java.util.Collection;
import litebans.dG;
import litebans.hl;
import litebans.hy_0;

public final class cc
implements Collection,
dG {
    private static /* synthetic */ String[] a;

    public static final byte a(byte[] byArray, int n) {
        return hy_0.a(byArray[n]);
    }

    public static int a(byte[] byArray) {
        return byArray.length;
    }

    private static final void a() {
        a = new String[]{"elements", "elements", "UByteArray(storage=", "Operation is not supported for read-only collection", "Operation is not supported for read-only collection", "Operation is not supported for read-only collection", "Operation is not supported for read-only collection", "Operation is not supported for read-only collection", "Operation is not supported for read-only collection", "storage", "Operation is not supported for read-only collection", "array"};
    }

    static {
        cc.a();
    }
}

